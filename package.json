{
    "name": "pokedex-json-server",
    "version": "0.0.1",
    "license": "ISC",
    "scripts": {
        "start": "json-server --watch tmp/db.json",
        "dev": "json-server --watch tmp/db.json",
        "now:deploy": "now"
    },
    "dependencies": {
        "json-server": "^0.16.1"
    }
}